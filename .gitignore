node_modules/
dist/
out/
*.log
logs/
.DS_Store
Thumbs.db

# lokale Laufzeitdaten (SQLite-Datenbank, Einstellungen, Backups) landen im
# Benutzerprofil (userData), nicht im Projekt – hier nur zur Sicherheit
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm
backups/
config/

# EXPERIMENTELL (Branch feature/perpustakaan-live-db): Java-Laufzeit +
# Derby-Jars für den Live-Zugriff, bei jedem Build per
# scripts/setup-derby-runtime.js neu geholt (wie Electrons eigene
# Binärdateien) – gehört nicht ins Repository, ~150 MB je Plattform.
derby-runtime/
